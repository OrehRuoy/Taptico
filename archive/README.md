Unused or leftover art kept for possible later use. Godot ignores this folder (`.gdignore`), so these files are not imported or packed into the app.

Do not reference anything here from scenes or scripts unless you move it back into `assets/` first.
