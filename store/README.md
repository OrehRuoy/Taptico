App Store screenshots, preview video stills, ASO copy, and listing art live here.

This folder is ignored by Godot (`.gdignore`) and excluded from the iOS export, so nothing here is packed into the IPA. Keep store marketing files here — never under `assets/`.
